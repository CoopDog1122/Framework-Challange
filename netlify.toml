[build]
  # No build command — this is a plain static site. Publish the repository root where index.html lives.
  publish = "."

[dev]
  # Local dev server (optional)
  command = "python3 -m http.server 8888"
  port = 8888
